# Module 2 — Demo 4: Kubernetes deploy commands

Run from the directory that contains your `kustomization.yaml` (or the root of your manifest layout).

## Deploy API to Kubernetes

```bash
kubectl apply -k .
```
